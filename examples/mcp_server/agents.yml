system:
  name: "Echo Agent"

defaults:
  model:
    provider: anthropic
    name: claude-haiku-4-5
    temperature: 0.0

execution:
  max_iterations: 5
  max_tool_calls: 3

agents:
  echo_agent:
    name: "Echo Agent"
    description: "A simple echo agent for demonstration."
    auto: true

graph:
  echo_agent:

